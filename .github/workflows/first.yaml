name: my first workflow in action

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step1 
        run: echo "welcome to job1"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: step2
        run : | 
              echo "my step2 execution"
              hostname
              date
 
